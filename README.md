# timetableoptimizer
Student Performance and Well-being Optimizer (SPWO) 📊 | A dynamic, constraint-based desktop application built with TypeScript/Electron that uses an $O(V+A)$ shortest path algorithm to generate personalized weekly schedules. It minimizes cognitive load and maximizes skill development consistency for students.
