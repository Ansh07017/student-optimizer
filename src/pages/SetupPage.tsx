import React from 'react';
import PriorityForm from '../components/PriorityForm/PriorityForm';

const SetupPage: React.FC = () => (
  <div>
    <h1>Setup</h1>
    <PriorityForm />
    {/* Add sleep window, classes, etc. */}
  </div>
);

export default SetupPage;
